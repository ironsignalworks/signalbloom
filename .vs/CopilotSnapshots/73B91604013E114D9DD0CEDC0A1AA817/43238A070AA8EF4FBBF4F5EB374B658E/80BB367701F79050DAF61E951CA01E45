<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generate Card Image</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            background: #111;
            font-family: 'Consolas', monospace;
        }
        
        #card {
            width: 1200px;
            height: 630px;
            background: #000;
            position: relative;
            overflow: hidden;
        }
        
        .gradient-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 50% 50%, rgba(0, 255, 136, 0.15) 0%, transparent 70%);
        }
        
        .content {
            position: relative;
            z-index: 2;
            padding: 80px;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 40px;
        }
        
        .icon {
            width: 80px;
            height: 80px;
        }
        
        .circle {
            fill: none;
            stroke: #00ff88;
            stroke-width: 2;
        }
        
        .dot {
            fill: #00ff88;
        }
        
        h1 {
            color: #00ff88;
            font-size: 72px;
            margin: 0;
            letter-spacing: 4px;
            text-transform: uppercase;
        }
        
        .subtitle {
            color: #888;
            font-size: 32px;
            margin: 20px 0 40px 0;
            letter-spacing: 2px;
        }
        
        .features {
            display: flex;
            gap: 30px;
            flex-wrap: wrap;
        }
        
        .feature {
            background: rgba(0, 255, 136, 0.05);
            border: 1px solid #222;
            padding: 15px 25px;
            color: #00ff88;
            font-size: 18px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }
        
        .particles {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 1;
        }
        
        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: #00ff88;
            border-radius: 50%;
            opacity: 0.3;
        }
        
        button {
            margin: 20px 0;
            padding: 15px 30px;
            background: #00ff88;
            border: none;
            color: #000;
            font: inherit;
            font-size: 16px;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>
    <div id="card">
        <div class="gradient-bg"></div>
        <div class="particles" id="particles"></div>
        <div class="content">
            <div class="logo">
                <svg class="icon" viewBox="0 0 80 80">
                    <circle cx="40" cy="40" r="35" class="circle"/>
                    <circle cx="40" cy="40" r="20" class="circle" opacity="0.5"/>
                    <circle cx="40" cy="40" r="5" class="dot"/>
                    <line x1="40" y1="5" x2="40" y2="15" class="circle"/>
                    <line x1="40" y1="75" x2="40" y2="65" class="circle"/>
                    <line x1="5" y1="40" x2="15" y2="40" class="circle"/>
                    <line x1="75" y1="40" x2="65" y2="40" class="circle"/>
                </svg>
                <h1>Signal Bloom</h1>
            </div>
            <div class="subtitle">Audio-Reactive 3D Visualizer</div>
            <div class="features">
                <div class="feature">5 Visualization Modes</div>
                <div class="feature">Real-Time Audio</div>
                <div class="feature">Three.js + WebGL</div>
            </div>
        </div>
    </div>
    
    <button onclick="downloadCard()">Download Card (1200x630)</button>
    <button onclick="downloadFavicon32()">Download Favicon 32x32</button>
    <button onclick="downloadFavicon16()">Download Favicon 16x16</button>
    
    <script>
        // Add particles
        const particlesContainer = document.getElementById('particles');
        for (let i = 0; i < 100; i++) {
            const particle = document.createElement('div');
            particle.className = 'particle';
            particle.style.left = Math.random() * 100 + '%';
            particle.style.top = Math.random() * 100 + '%';
            particle.style.opacity = Math.random() * 0.5;
            particlesContainer.appendChild(particle);
        }
        
        async function downloadCard() {
            const card = document.getElementById('card');
            const canvas = await html2canvas(card, {
                width: 1200,
                height: 630,
                backgroundColor: '#000000'
            });
            
            const link = document.createElement('a');
            link.download = 'card.png';
            link.href = canvas.toDataURL('image/png');
            link.click();
        }
        
        async function downloadFavicon32() {
            const svg = createFaviconSVG(32);
            const canvas = document.createElement('canvas');
            canvas.width = 32;
            canvas.height = 32;
            const ctx = canvas.getContext('2d');
            
            const img = new Image();
            img.onload = () => {
                ctx.drawImage(img, 0, 0);
                const link = document.createElement('a');
                link.download = 'favicon-32x32.png';
                link.href = canvas.toDataURL('image/png');
                link.click();
            };
            img.src = 'data:image/svg+xml;base64,' + btoa(svg);
        }
        
        async function downloadFavicon16() {
            const svg = createFaviconSVG(16);
            const canvas = document.createElement('canvas');
            canvas.width = 16;
            canvas.height = 16;
            const ctx = canvas.getContext('2d');
            
            const img = new Image();
            img.onload = () => {
                ctx.drawImage(img, 0, 0);
                const link = document.createElement('a');
                link.download = 'favicon-16x16.png';
                link.href = canvas.toDataURL('image/png');
                link.click();
            };
            img.src = 'data:image/svg+xml;base64,' + btoa(svg);
        }
        
        function createFaviconSVG(size) {
            return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${size} ${size}" width="${size}" height="${size}">
                <rect width="${size}" height="${size}" fill="#000"/>
                <circle cx="${size/2}" cy="${size/2}" r="${size*0.3}" stroke="#00ff88" stroke-width="${size*0.06}" fill="none"/>
                <circle cx="${size/2}" cy="${size/2}" r="${size*0.15}" fill="#00ff88"/>
            </svg>`;
        }
    </script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
</body>
</html>
